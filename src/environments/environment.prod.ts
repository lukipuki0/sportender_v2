export const environment = {
  production: true,
  // Añade la URL de tu backend de producción aquí
  // Esta URL DEBE ser diferente a la de desarrollo si despliegas en otro lugar
  apiUrl: 'YOUR_PRODUCTION_BACKEND_URL/api' // CAMBIA ESTO a la URL de tu backend en producción
};
